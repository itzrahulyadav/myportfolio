##Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved.
##SPDX-License-Identifier: MIT-0
version: 0.2
phases:
  install:
    commands:
      - echo "install phase....."
  pre_build:
    commands:
      - docker --version
      - docker pull owasp/zap2docker-stable
      - docker run -t owasp/zap2docker-stable zap-baseline.py -t http://itzrahulyadav.s3-website.ap-south-1.amazonaws.com  
  build:
    commands: 
      - echo "build finished"
  post_build:
    commands:
      - echo "post build finished"
